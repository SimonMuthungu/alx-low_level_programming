#include "main.h" /*main file header*/

/**
 * main- calls a function
 *
 * Return 0
 *
 */

void print_alphabet(void)
{
	for (i = 'a'; i <= 'z'; i++);
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
